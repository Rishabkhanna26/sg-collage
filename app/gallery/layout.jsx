export const metadata = {
  title: "Gallery — SQ Group of Colleges",
  description:
    "Browse photos from SQ Group of Colleges — department activities, campus events, college life, and annual celebrations across the years.",
};

export default function GalleryLayout({ children }) {
  return children;
}
